# CodeRabbit: Настройка и Использование

## ❗ ВАЖНО: Что такое CodeRabbit

Согласно [официальному сайту](https://www.coderabbit.ai/), CodeRabbit — это:
- **SaaS-инструмент** для автоматического AI-код-ревью через **GitHub/GitLab Pull Requests**
- Самое популярное приложение на GitHub и GitLab (2M репо, 13M PR)
- Поддержка IDE (VS Code, Cursor, Windsurf)
- Контекстные AI‑ревью, интеграция с workflow, автоматические отчёты

## 🎯 Основные варианты использования

### 1. GitHub / GitLab App (Рекомендуется)

Судя по [официальному сайту](https://www.coderabbit.ai/), CodeRabbit — это **AI App на GitHub/GitLab**:

1. **Установка**:
   - Регистрация на https://coderabbit.ai
   - Пробный период 14 дней
   - Без карты
   - Регистрация через GitHub/GitLab

2. **Установка в репозиторий**:
   - В настройках репо GitHub/GitLab установить CodeRabbit App
   - Выбрать `seshb0y/Bots`
   - Дать нужные права

3. **Как работает**:
   - Анализирует каждый Pull Request
   - Построчный AI‑ревью
   - Исправления с кликом
   - Работает в фоне

### 2. IDE Reviews (VS Code / Cursor / Windsurf)

Бесплатные AI‑ревью прямо в редакторе: https://www.coderabbit.ai/

1. **Расширение**:
   - Установить расширение CodeRabbit
   - Поддержка VS Code, Cursor, Windsurf и других форков

2. **Активация**:
   - Открыть файл в редакторе
   - CodeRabbit анализирует код
   - Показать инлайн‑подсказки
   - Проверка staged/unstaged коммитов
   - Бесплатно, с лимитами

3. **Настройка**:
   ```bash
   # В Cursor/VS Code Settings
   # Ищем "CodeRabbit" в настройках
   # Подключаем аккаунт GitHub
   ```

### 3. CLI Reviews (ВОТ ЭТО!)

Согласно [официальному сайту CLI](https://www.coderabbit.ai/cli), CodeRabbit имеет **НАСТОЯЩИЙ терминальный CLI**:

#### Установка:
```bash
curl -fsSL https://cli.coderabbit.ai/install.sh | sh
```

Работает в: **macOS, Linux, Windows (WSL)**.

⚠️ **ВАЖНО**: Требует современный CPU с поддержкой AVX2.
- ✅ Работает: Haswell+ (2013+), Ryzen (2017+)
- ❌ НЕ работает: Sandy Bridge (2011), Ivy Bridge (2012), старые Xeon
- Проверка: `grep avx2 /proc/cpuinfo`
- Если не работает: используй GitHub App или IDE расширение

#### Что умеет:
- Ревью незакоммиченных изменений (staged и unstaged)
- Построчный AI‑анализ кода
- Поиск багов и уязвимостей до коммита
- Интеграция с AI‑ассистентами (Claude Code, Cursor CLI, Gemini)
- Однокликовые фиксы прямо в терминале
- Бесплатные ревью в CLI (с лимитами)

#### Основная команда:
```bash
coderabbit review --plain  # Ревью всех изменений
```

#### Как это работает:
1. `coderabbit review` — анализирует незакоммиченные изменения
2. Показывает построчные замечания
3. Ты фиксишь проблемы до создания PR
4. Меньше комментов в PR

#### Для твоего discord-bot:
```bash
cd /opt/discord-bot
coderabbit review --plain  # Проанализирует весь код
```

## 🔧 Что НЕ работает

CodeRabbit CLI требует:
- **Интернет-соединение** — SaaS на их серверах
- **Git репозиторий** — работает только в Git‑проектах
- **Свободный plan** — бесплатно, но с лимитами

## ✅ Альтернативы для локального анализа

Если тебе нужен **локальный статический анализ** с AI, используй:

### 1. **ESLint** + TypeScript
```bash
npm install -D @typescript-eslint/eslint-plugin @typescript-eslint/parser
# Создай .eslintrc.js
```

### 2. **SonarLint** (тоже VS Code расширение)
- Работает локально
- Находит баги и уязвимости
- Бесплатно

### 3. **DeepSource** CLI
```bash
# Есть локальный CLI
deepsource init
deepsource scan
```

## 📝 Для твоего discord-bot проекта

### Если используешь CodeRabbit через GitHub:

1. **Создай Pull Request**:
   ```bash
   git checkout -b feature/test-coderabbit
   # Делаешь изменения
   git add .
   git commit -m "Test code changes"
   git push origin feature/test-coderabbit
   ```

2. **Создай PR на GitHub**:
   - Иди на https://github.com/seshb0y/Bots/pulls
   - Нажми "New Pull Request"
   - Выбери ветку с изменениями
   - Открывай PR

3. **CodeRabbit автоматически**:
   - Проанализирует изменения
   - Оставит комментарии в PR
   - Предложит исправления

### Если используешь через Cursor:

1. **Открой файл** (например, `src/bot.ts`)
2. CodeRabbit покажет:
   - Инлайн-подсказки
   - Предупреждения о безопасности
   - Стилистические проблемы
   - Оптимизации

## 🔍 Проверка текущей настройки

Проверь, установлен ли CodeRabbit в репозитории:

```bash
cd /opt/discord-bot
# Проверяем GitHub App установки (нужен доступ к GitHub)
```

Или проверь в GitHub:
1. https://github.com/seshb0y/Bots/settings/installations
2. Ищи "CodeRabbit" в списке установленных приложений

## 🎓 Резюме

**CodeRabbit = AI-ревью для GitHub PR**, а не локальный CLI.

Для локального анализа используй:
- ESLint + TypeScript
- SonarLint
- VS Code встроенный анализатор

Для автоматического ревью PR используй:
- CodeRabbit GitHub App
- Cursor/VS Code расширение с GitHub интеграцией

## 📞 Что сделать дальше?

### 🚀 Если хочешь начать с CodeRabbit ПРЯМО СЕЙЧАС:

#### Вариант A: CLI (рекомендуется)

1. **Установка CLI**:
   ```bash
   curl -fsSL https://cli.coderabbit.ai/install.sh | sh
   ```

2. **Проверка совместимости**:
   ```bash
   # Сначала проверь, что твой CPU поддерживает AVX2:
   grep avx2 /proc/cpuinfo
   
   # Если avx2 НЕ найден → CLI не будет работать
   # Используй вариант B (GitHub App) вместо этого!
   ```

3. **Проверка установки**:
   ```bash
   coderabbit --version  # Проверь, что установлено
   ```

4. **Первый запуск**:
   ```bash
   cd /opt/discord-bot
   coderabbit review --plain  # Проанализирует весь код!
   ```

5. **Что будет**:
   - Покажет построчные замечания
   - Предложит исправления
   - Запрос на регистрацию при первом запуске

#### Вариант B: GitHub App

1. **Регистрация**:
   ```bash
   # Открой в браузере:
   https://coderabbit.ai
   ```
   - Нажми "Get Started" или "Get a free trial"
   - Регистрация через GitHub
   - 14 дней бесплатно, без карты

2. **Установка в репо**:
   - На главной странице нажми "Install on GitHub"
   - Выбери репозиторий `seshb0y/Bots`
   - Дать нужные права

3. **Создай тестовый PR**:
   ```bash
   cd /opt/discord-bot
   git checkout -b test-coderabbit
   # Сделай минимальное изменение
   git add .
   git commit -m "Test: try CodeRabbit"
   git push origin test-coderabbit
   ```
   - На GitHub создай PR из ветки `test-coderabbit`
   - CodeRabbit автоматически проанализирует изменения

### 🔧 Если хочешь локальный анализ:

Я настрою ESLint + TypeScript или другой анализатор.

### 🎯 Резюме выбора:

- **CLI‑ревью в терминале** → CodeRabbit CLI (`curl -fsSL https://cli.coderabbit.ai/install.sh | sh`)
- **Автоматическое ревью PR** → CodeRabbit GitHub App
- **Бесплатные IDE‑ревью** → CodeRabbit расширение для Cursor/VS Code
- **Локальный анализ без интернета** → ESLint/SonarLint/TypeScript

**Рекомендация**: 
- Если твой CPU поддерживает AVX2 → начни с **CLI** (быстро, удобно, в терминале)
- Если старый CPU (Ivy Bridge и старше) → используй **GitHub App** или **IDE расширение**

