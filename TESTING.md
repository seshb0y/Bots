# 🧪 Система тестирования Discord бота

## Обзор

Проект включает комплексную систему тестирования, которая обеспечивает надежность и стабильность работы бота. Система состоит из нескольких компонентов:

- **Предстартовые тесты** - выполняются при запуске бота
- **Автоматическое тестирование** - мониторинг изменений файлов
- **Ручное тестирование** - команды для запуска тестов
- **Специализированные тесты** - для конкретных функций

## 📋 Наборы тестов

### 1. Конфигурация (5 тестов)
- ✅ Переменные окружения
- ✅ Директории данных
- ✅ Директории логов
- ✅ Версия Node.js
- ✅ package.json

### 2. Утилиты (5 тестов)
- ✅ JSON утилиты
- ✅ Нормализация строк
- ✅ Система логирования
- ✅ Константы
- ✅ TypeScript типы

### 3. Команды (5 тестов)
- ✅ Импорты команд
- ✅ Команда help
- ✅ Команда points
- ✅ Команда addtracer
- ✅ Регистрация команд

### 4. Данные (5 тестов)
- ✅ Структуры данных
- ✅ Сохранение данных
- ✅ Миграция данных
- ✅ Валидация данных
- ✅ Резервное копирование

### 5. Сеть (7 тестов)
- ✅ Подключение к Discord API
- ✅ Подключение к сайту War Thunder
- ✅ Подключение к ThunderSkill
- ✅ Парсинг данных клана
- ✅ Обработка таймаутов сети
- ✅ Повторные попытки при ошибках сети
- ✅ HTTP заголовки и User-Agent

### 6. Статистика (4 теста)
- ✅ Файлы данных статистики
- ✅ Сравнение данных статистики
- ✅ Согласованность данных статистики
- ✅ Обработка пустых данных статистики

## 🚀 Запуск тестов

### Автономное тестирование
```bash
npm test
```

### Тестирование с сборкой
```bash
npm run test:build
```

### Безопасный запуск (тесты + запуск)
```bash
npm run start:safe
```

## 🔄 Автоматическое тестирование

### Сервис автоматического тестирования

Бот включает сервис автоматического тестирования, который:

- **Мониторит файлы** в реальном времени
- **Запускает тесты** при изменении кода
- **Ведет историю** результатов тестирования
- **Ограничивает частоту** тестов для производительности

### Настройка мониторинга

По умолчанию мониторятся следующие пути:
- `src/commands/` - команды бота
- `src/utils/` - утилиты
- `src/tests/` - тесты
- `src/bot.ts` - основной файл бота
- `src/index.ts` - точка входа
- `src/constants.ts` - константы
- `src/types.ts` - типы

### Исключения

Следующие файлы и папки исключены из мониторинга:
- `node_modules/`
- `.git/`
- `dist/`
- `logs/`
- `*.log`
- `*.tmp`

## 🎮 Команды Discord

### `/autotest` - Управление автоматическим тестированием

#### Подкоманды:

**`/autotest status`** - Показать статус сервиса
- Количество запущенных тестов
- Текущий статус (выполняется/ожидает)
- Время работы сервиса
- Время последнего теста

**`/autotest run`** - Принудительно запустить тесты
- Выполняет полный набор тестов
- Показывает детальные результаты
- Отображает ошибки (если есть)

**`/autotest start`** - Запустить сервис мониторинга
- Включает автоматическое тестирование
- Начинает мониторинг файлов

**`/autotest stop`** - Остановить сервис мониторинга
- Отключает автоматическое тестирование
- Останавливает мониторинг файлов

**`/autotest history [limit]`** - Показать историю тестов
- Отображает последние результаты тестирования
- Параметр `limit` (1-50, по умолчанию 10)

### `/runtests` - Ручное тестирование
- Запускает все тесты
- Показывает результаты в Discord
- Доступно только администраторам

## 📊 История тестирования

### Файл истории
Результаты тестов сохраняются в `/opt/data/test_history.json`:

```json
[
  {
    "timestamp": "2025-09-07T15:02:32.000Z",
    "reason": "file-change",
    "changedFile": "/opt/discord-bot/src/bot.ts",
    "success": true,
    "passed": 31,
    "total": 31,
    "duration": 8500,
    "errors": [],
    "suiteResults": {
      "Конфигурация": { "passed": 5, "total": 5 },
      "Утилиты": { "passed": 5, "total": 5 },
      "Команды": { "passed": 5, "total": 5 },
      "Данные": { "passed": 5, "total": 5 },
      "Сеть": { "passed": 7, "total": 7 },
      "Статистика": { "passed": 4, "total": 4 }
    }
  }
]
```

### Ограничения истории
- Сохраняется максимум 100 последних записей
- Старые записи автоматически удаляются

## ⚙️ Конфигурация

### Параметры автоматического тестирования

```typescript
interface AutoTestConfig {
  watchPaths: string[];        // Пути для мониторинга
  excludePatterns: string[];   // Паттерны исключений
  testOnStart: boolean;        // Тестировать при запуске
  testOnChange: boolean;       // Тестировать при изменениях
  debounceMs: number;          // Задержка перед тестом (мс)
  maxTestFrequency: number;    // Максимальная частота (тестов/мин)
}
```

### Настройки по умолчанию
- **Задержка**: 2000мс (2 секунды)
- **Максимальная частота**: 10 тестов в минуту
- **Тестирование при запуске**: включено
- **Тестирование при изменениях**: включено

## 🔧 Разработка тестов

### Создание нового теста

1. **Создайте файл теста** в `src/tests/`:
```typescript
import { TestResult } from './testRunner';

async function testMyFeature(): Promise<TestResult> {
  const start = Date.now();
  const name = "Мой тест";
  
  try {
    // Логика теста
    return {
      name,
      success: true,
      duration: Date.now() - start
    };
  } catch (error) {
    return {
      name,
      success: false,
      error: `Ошибка: ${error}`,
      duration: Date.now() - start
    };
  }
}

export const myTests = [testMyFeature];
```

2. **Добавьте в testRunner.ts**:
```typescript
import { myTests } from './myTests';

const testSuites: TestSuite[] = [
  // ... существующие наборы
  { name: "Мои тесты", tests: myTests }
];
```

### Структура TestResult

```typescript
interface TestResult {
  name: string;        // Название теста
  success: boolean;    // Успешность
  error?: string;      // Сообщение об ошибке
  duration: number;    // Время выполнения (мс)
}
```

## 🚨 Обработка ошибок

### Типы ошибок

1. **Ошибки конфигурации** - отсутствие переменных окружения
2. **Ошибки импорта** - проблемы с модулями
3. **Ошибки сети** - недоступность внешних сервисов
4. **Ошибки данных** - проблемы с файлами данных
5. **Ошибки логики** - неправильная работа функций

### Логирование

Все ошибки логируются в:
- Консоль (при автономном запуске)
- Файлы логов (`logs/`)
- Discord (при использовании команд)

## 📈 Мониторинг производительности

### Метрики тестирования

- **Время выполнения** каждого теста
- **Общее время** выполнения набора тестов
- **Частота запусков** тестов
- **Процент успешности** тестов

### Оптимизация

- **Debouncing** - предотвращает частые запуски
- **Ограничение частоты** - максимум 10 тестов в минуту
- **Параллельное выполнение** - тесты запускаются параллельно
- **Кэширование** - результаты сохраняются для анализа

## 🔒 Безопасность

### Права доступа

- **`/runtests`** - только администраторы
- **`/autotest`** - только администраторы
- **Автоматическое тестирование** - работает в фоне

### Защита данных

- Тестовые данные создаются во временных файлах
- Автоматическая очистка после тестов
- Не влияет на продакшн данные

## 🎯 Лучшие практики

### При разработке

1. **Запускайте тесты** перед коммитом
2. **Исправляйте ошибки** немедленно
3. **Добавляйте тесты** для новых функций
4. **Мониторьте историю** тестирования

### При деплое

1. **Проверьте статус** автоматического тестирования
2. **Запустите полные тесты** перед деплоем
3. **Мониторьте логи** после деплоя
4. **Проверьте работоспособность** основных функций

## 📞 Поддержка

### Диагностика проблем

1. **Проверьте логи** в `logs/`
2. **Запустите тесты** вручную
3. **Проверьте конфигурацию** переменных окружения
4. **Убедитесь в доступности** внешних сервисов

### Восстановление

1. **Перезапустите бота** для сброса состояния
2. **Очистите кэш** тестов
3. **Проверьте целостность** файлов данных
4. **Восстановите из резервной копии** при необходимости

---

**Система тестирования обеспечивает высокое качество и надежность Discord бота, автоматически отслеживая изменения и предотвращая проблемы в продакшене.**
