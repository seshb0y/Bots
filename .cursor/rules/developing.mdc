---
description:
globs:
alwaysApply: true
---

# Правила Cursor — SOLID, Паттерны, Тесты, Документация (.mdc)

> **Цель**: Настроить Cursor так, чтобы код всегда соответствовал принципам SOLID, использовал архитектурные и проектные паттерны, *обязательно* сопровождался тестами и документацией (README, ADR, docstring).
>
> **Область применения**: PHP/Bitrix (PHPUnit), TypeScript/Node/Next/Nuxt (Vitest/Jest), Python/FastAPI (pytest), C#/.NET (xUnit). Если проект использует другие инструменты — придерживаться их.

---

## ✅ Определение готовности (DoD)

Изменение считается завершённым, если:

1. Архитектура соответствует SOLID и использует паттерны с пояснением выбора.
2. Добавлены или обновлены тесты (юнит + интеграционные, если требуется).
3. Покрытие тестами не снижается.
4. Обновлена документация (README, ADR, комментарии).
5. Все проверки CI (линтеры, форматтеры, тесты) проходят успешно.
6. Выполнен чеклист безопасности и производительности.

Если что-то невозможно выполнить, нужно зафиксировать TODO с обоснованием и сроком.

---

## 🧠 Принципы SOLID

* **S** — Single Responsibility: один повод для изменения.
* **O** — Open/Closed: расширение через абстракции, а не изменение кода.
* **L** — Liskov Substitution: подтипы можно безопасно подставлять.
* **I** — Interface Segregation: узкие интерфейсы вместо общих.
* **D** — Dependency Inversion: работа через абстракции, внедрение зависимостей.

Предпочтительно:

* Использовать **композицию** вместо наследования.
* Разделять систему на **bounded contexts**.
* Применять **гексагональную архитектуру (Порты и Адаптеры)**.
* Использовать **CQRS**, если чтение и запись сильно различаются.

---

## 🧱 Архитектурные правила

* Новая фича → сначала определить **границы модулей**.
* Весь IO (БД, HTTP, Bitrix API) скрывать за **адаптерами**.
* Доменные сущности **не должны** зависеть от фреймворков.
* Использовать:

  * **Factory/Builder** для сложных объектов.
  * **Strategy/Policy** для ветвящейся логики.
  * **Template Method/State** для вариантов процессов.
  * **Decorator** для кросс-среза (логирование, кэш).
  * **Domain Events/Observer** для побочных эффектов.

Пример структуры:

```
src/
  domain/            # Сущности, Value Object, доменные сервисы, события
  application/       # UseCase, команды/запросы, DTO, обработчики
  infrastructure/    # Репозитории, адаптеры (БД/HTTP/Bitrix)
  interfaces/        # Контроллеры, REST/GraphQL, CLI, UI
  config/            # DI контейнер
  shared/            # Общие утилиты (Result, Guard, Logger)
```

---

## 🧩 Когда применять паттерны

* Слишком много if/else → **Strategy** или **Chain of Responsibility**.
* Настраиваемый процесс → **Template Method** или **State**.
* Кросс-срез (кэш, логирование) → **Decorator**.
* Несколько источников данных → **Repository + Adapter**.
* Дорогая инициализация → **Factory/Builder**.
* Асинхронные эффекты → **Domain Events/Observer**.
* Внешние API → **Gateway + Anti-Corruption Layer**.

В PR писать: *«Паттерн: Strategy, потому что … Рассматривались альтернативы: …»*

---

## 🧪 Правила тестирования

1. Следовать циклу **Red → Green → Refactor**.
2. Покрывать тестами все новые публичные методы.
3. Для адаптеров — писать **контрактные тесты**.
4. Делать интеграционные тесты для БД/HTTP/Bitrix.
5. Давать тестам имена по **поведению**, а не по реализации.

Минимальный набор тестов:

* Нормальные сценарии
* Граничные случаи
* Ошибки и исключения
* Идемпотентность и конкурентность (если актуально)
* Проверка безопасности (авторизация, валидация)

Инструменты:

* PHP: PHPUnit / Pest
* JS/TS: Vitest / Jest
* Python: pytest
* C#: xUnit

---

## 📚 Документация

Каждое изменение должно сопровождаться:

* Обновлением **README** (описание, запуск, тестирование, конфигурация).
* Обновлением/созданием **ADR**: `docs/adrs/NNN-title.md`.
* Добавлением docstring/JSDoc/PHPDoc.
* Для API — пример вызова (curl, usage).

README должно включать:

* Введение и схему архитектуры
* Быстрый старт (установка, запуск)
* Конфигурацию (env vars)
* Инструкции по контрибьюции
* Лицензию и контакты для безопасности

---

## 🔐 Чеклист безопасности

* Проверять и фильтровать все входные данные.
* Минимизировать права для ключей и токенов.
* Секреты хранить в env/secret manager.
* Настраивать таймауты, ретраи и circuit-breaker для внешних сервисов.
* Логировать без PII.
* Тестировать авторизацию и утечки.

---

## 🚀 Чеклист производительности

* Избегать N+1 запросов.
* Использовать кэш (in-memory, Redis).
* Добавлять метрики.
* Использовать пагинацию/стриминг для больших данных.

---

## 🧰 Workflow разработки

1. **Планирование**

   * Выделить требования и acceptance criteria.
   * Сначала спроектировать архитектуру и тесты.
2. **Скелет**

   * Создать интерфейсы и заглушки.
   * Подготовить тесты.
3. **Реализация**

   * Сначала доменная логика, потом адаптеры.
   * Маленькие функции (<30 строк).
4. **Тесты**

   * Unit, потом integration.
   * Покрытие не падает.
5. **Документация**

   * Обновить README и ADR.
6. **Коммиты/PR**

   * Использовать Conventional Commits.
   * Заполнять чеклист PR.

---

## 🧾 Стиль коммитов

* `feat: …` — новая фича
* `fix: …` — исправление бага
* `docs: …` — документация
* `test: …` — тесты
* `refactor: …` — рефакторинг
* `perf: …` — оптимизация
* `chore: …` — инфраструктура
